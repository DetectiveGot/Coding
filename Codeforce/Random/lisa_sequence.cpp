#include<bits/stdc++.h>

using namespace std;

const int N = 1e6+2;
int a[N],b[N], dp[N];

int main(){
	ios::sync_with_stdio(0); cin.tie(0);
	int n,m; cin >> n >> m;
	for(int i=1;i<=n;i++) cin >> a[i];
	
	
	return 0;
}
